version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/ffreis/hello
