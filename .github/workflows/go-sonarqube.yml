name: Go SonarQube

on:
  workflow_call:
    inputs:
      go-version:
        description: "Go version"
        required: false
        type: string
        default: "1.25"
      working-directory:
        description: "Directory containing the Go module"
        required: false
        type: string
        default: "."
      sonar-project-key:
        description: "SonarQube project key (defaults to repository name)"
        required: false
        type: string
        default: ""
      sonar-host-url:
        description: "SonarQube server URL (include scheme, e.g. https://sonarqube.example.com). Prefer setting as repo var SONAR_HOST_URL."
        required: false
        type: string
        default: ""
    secrets:
      SONAR_TOKEN:
        required: true
      SONAR_HOST_URL:
        required: false

permissions:
  contents: read

jobs:
  sonar:
    name: SonarQube
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository

    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: ${{ inputs.go-version }}
          cache: true
          cache-dependency-path: ${{ inputs.working-directory }}/go.sum

      - name: Validate SonarQube token
        env:
          SONAR_TOKEN_INPUT: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "$SONAR_TOKEN_INPUT" ]]; then
            echo "::error::Missing required secret SONAR_TOKEN"
            exit 1
          fi

      - name: Generate coverage
        run: |
          set -euo pipefail
          go test ./... \
            -coverprofile=coverage.out \
            -covermode=atomic \
            -count=1 \
            -timeout=120s

      - name: Build Sonar args
        id: sonar-args
        env:
          PROJECT_KEY: ${{ inputs.sonar-project-key }}
        run: |
          set -euo pipefail
          extra=""
          if [ -n "$PROJECT_KEY" ]; then
            extra="$extra -Dsonar.projectKey=$PROJECT_KEY"
          fi
          echo "args=$extra" >> "$GITHUB_OUTPUT"

      - name: Validate SonarQube host URL
        id: host
        env:
          SONAR_HOST_URL_SECRET: ${{ secrets.SONAR_HOST_URL }}
          SONAR_HOST_URL_INPUT: ${{ inputs.sonar-host-url }}
          SONAR_HOST_URL_VAR: ${{ vars.SONAR_HOST_URL }}
        run: |
          set -euo pipefail
          host_url="${SONAR_HOST_URL_SECRET:-}"
          if [[ -z "$host_url" ]]; then
            host_url="${SONAR_HOST_URL_INPUT:-}"
          fi
          if [[ -z "$host_url" ]]; then
            host_url="${SONAR_HOST_URL_VAR:-}"
          fi
          if [[ -z "$host_url" ]]; then
            echo "::error::Missing SonarQube host URL. Set repo var SONAR_HOST_URL or pass input sonar-host-url (or secret SONAR_HOST_URL)."
            exit 1
          fi
          if [[ "$host_url" != http://* && "$host_url" != https://* ]]; then
            echo "::error::SonarQube host URL must include scheme (http:// or https://). Got: $host_url"
            exit 1
          fi
          echo "url=$host_url" >> "$GITHUB_OUTPUT"

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9 # v7.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ steps.host.outputs.url }}
        with:
          projectBaseDir: ${{ inputs.working-directory }}
          args: >
            ${{ steps.sonar-args.outputs.args }}
            -Dsonar.go.coverage.reportPaths=coverage.out
            -Dsonar.sources=.
            -Dsonar.exclusions=vendor/**,**/*_test.go
            -Dsonar.tests=.
            -Dsonar.test.inclusions=**/*_test.go
